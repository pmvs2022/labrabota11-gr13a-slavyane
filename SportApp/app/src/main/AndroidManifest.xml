<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportApp"
        tools:targetApi="31">

        <receiver android:name=".ReminderBroadcast"/>

        <activity android:name=".DayDoneTrainingsActivity"
            android:label="@string/back_to_main_page"
            android:theme="@style/Theme.WithActionBar"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".TrainingOverActivity"
            android:label="@string/back_to_main_page"
            android:theme="@style/Theme.WithActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ExercisesPipelineActivity"
            android:label="@string/training"
            android:screenOrientation="portrait"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".TrainingIntroActivity"
            android:label="@string/training"
            android:theme="@style/Theme.WithActionBar"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".TypedTrainingsActivity"
            android:label="@string/trainings"
            android:theme="@style/Theme.WithActionBar"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

    </application>

</manifest>